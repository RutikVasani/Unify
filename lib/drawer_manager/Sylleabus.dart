import 'package:flutter/material.dart';

class Sylleabus extends StatelessWidget {
  const Sylleabus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text(
        "Sylleabus",
        style: TextStyle(fontSize: 30),
      ),
    ));
  }
}
